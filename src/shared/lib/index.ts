/** Public API of shared/lib — import from `@/shared/lib`, not deep paths. */
export { classNames } from './classNames';
export { generateId } from './id';
export { formatShortDate, formatRelativeTime } from './date';
